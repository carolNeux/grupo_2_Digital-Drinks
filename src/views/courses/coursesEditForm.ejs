<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Editar producto |Digital Drinks</title>
    <link rel="stylesheet" href="/stylesheets/styleCreateEdit.css" >
    <!-- href="/styles/style_reg.css"> -->
    <script src="https://kit.fontawesome.com/5376d63d7e.js" crossorigin="anonymous"></script>
    <meta name="description" content="cocktails, drinks, bar tools, herramientas de barra, cocteleras, cocktails recepies, recetas de cocteles, cursos de cocteleria">
</head>
<body>
    <!-- Aca va el Header -->
    <div class="reg-box">
        <a href="/courses/"> <img src="/images/header/logoAzul.svg" alt=""> </a>
        <h2> Edita tu Curso</h2>
        <form action="/courses/edit/<%= coursesDetail.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
            <% if (typeof errors != 'undefined') { %>
                <% errors.forEach(error => { %>
                 <ul class="errors">
                     <li> <%= error.msg %> </li>
                 </ul>
                <% }) %>
               <% } %>
        <div class="reg-fill">  
            <label for="category">Categoría:</label>
            <label for="name">Nombre</label>
            <input type="text" name="name" value = "<%= coursesDetail.name %>">
            <label for="price" >Precio :</label>
            <input type="text" id="price" name="price" value = "<%= coursesDetail.price %>" >
            <label for="discount">Descuento:</label>
            <input type="text" id="discount" name="discount" value = "<%= coursesDetail.discount %>" >
            <label for="description" class="">Descripción:</label>
            <textarea name="description" id="description" class="form-input"><%= coursesDetail.description %>"</textarea>
            <label for="image"> Agregar imagen</label>
            <div class="foto">  <img class="img"   src="/images/courses/<%= coursesDetail.image %>" alt="<%= coursesDetail.name %>"></div>
            <input type="file" name="image">
            <br>
            <button type="submit">Guardar Curso</button>
            <br>
            <p class="get_out"><a href="/courses" >Volver a los cursos</a></p>
            <br>
        </div>
    </form>
    </div>
<!-- Aca va el Footer -->   
</body>
</html>